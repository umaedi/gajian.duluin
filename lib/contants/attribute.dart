/* Scroll Padding */
const double SCROLL_PADDING_HORIZONTAL = 20;
const double SCROLL_PADDING_VERTICAL = 15;

/*Dio Attribute*/
const Duration CONNECT_TIMEOUT = Duration(seconds: 30);
const Duration RECEIVE_TIMEOUT = Duration(seconds: 30);
